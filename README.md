# Project Portfolio

The project is built with **Laravel** using **Docker** and a **REST API** architecture.  
Its goal is to provide a convenient, flexible, and scalable backend for web applications or external services.

---

## 🔧 Project Technologies

- **Laravel** — a modern PHP framework for backend development
- **Docker / Docker Compose** — containerization and service isolation
- **REST API** — standardized interface for client-server communication
- **PostgreSQL 13** — primary database 
- **Nginx (alpine)** — web server handling HTTP requests
- **PHP-FPM** — PHP execution layer based on a custom Docker image 
- **PHP 8.x** — PHP version defined in the PHP-FPM Dockerfile
- **Composer** — dependency management for PHP

---

## 🚀 Functionality

- Clean and well-structured API architecture 
- Modular controllers and service layers
- Proper request/response handling according to REST standards
- Support for middleware (e.g., authentication/authorization)
- Scalable infrastructure thanks to Dockerized services (webserver, php-fpm, pgsql)
- Environment isolation and simple deployment via Docker  
